<p id="notice"><%= notice %></p>
<div class=container>
      <p>
        <strong>Name:</strong>
        <%= @guest.name %>
      </p>
      <p>
        <strong>Comment:</strong>
        <%= @guest.comment %>
      </p>

      <%= link_to 'Edit', edit_guest_path(@guest), class: "btn btn-lg btn-primary" %> 
</div>
